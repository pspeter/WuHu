<Window x:Class="WuHu.Terminal.TerminalWindow"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WuHu.Terminal"
        xmlns:vm="clr-namespace:WuHu.Terminal.ViewModels"
        mc:Ignorable="d"
        Title="Terminal - WuHu" 
        Height="900" Width="800" SizeToContent="Width">

    <Window.DataContext>
        <vm:TerminalVm/>
    </Window.DataContext>

    <DockPanel Name="MenuList">
        <Menu IsMainMenu="True" DockPanel.Dock="Top">
            <MenuItem Header="_Rangliste" IsEnabled="{Binding IsAuthenticated}">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="Trophy"></materialDesign:PackIcon>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="_Spielpläne">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="FormatListBulleted"></materialDesign:PackIcon>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="_Rangliste">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="Trophy"></materialDesign:PackIcon>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="_Spieler">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="Account"></materialDesign:PackIcon>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="_Statistik">
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="TrendingUp"></materialDesign:PackIcon>
                </MenuItem.Icon>
            </MenuItem>
            <MenuItem>
                <MenuItem.Icon>
                    <materialDesign:PackIcon Kind="{Binding}"></materialDesign:PackIcon>
                </MenuItem.Icon>
            </MenuItem>
        </Menu>
        <ContentControl DockPanel.Dock="Left" Content="{Binding}"></ContentControl>
    </DockPanel>
</Window>
